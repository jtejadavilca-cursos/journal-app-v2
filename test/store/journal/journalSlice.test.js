// create journalSlice test

describe("journalSlice", () => {
    const initialState = {
        notes: [],
        active: null,
        isSaving: false,
        savedMessage: "",
    };
    it("should handle initial state", () => {});
});
